# pnpm 11 restricted .npmrc to auth and registry settings and ignores the rest
# silently. engine-strict and enable-pre-post-scripts moved to
# pnpm-workspace.yaml as engineStrict / enablePrePostScripts -- putting them
# back here would read as "off", not as an error.
